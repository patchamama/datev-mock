# DATEV Mock — Extended Endpoint Coverage

## Objective
Expand the mock from 3 endpoints to the ~22 covered by ELO's existing
internal DATEV mock (`C:\Mockup\serve-0.1-generate.jar`, see
[[project_internal_java_mock_reference]] and
`examples/DATEV_Mock_Server_Reference.md`, local-only). User decision
2026-09-22: build all of it, as a new epic, after the base API and
settings/admin epics (both already GREEN and shipped).

## Ground truth for this epic
- `examples/DATEV_Mock_Server_Reference.md` — local-only, endpoint list, example shapes, runtime behavior notes (query param quirks, 404-on-bad-addressee-id, etc.), extracted from the internal mock's JAR/bytecode/live responses.
- Official DATEV OpenAPI 3.0.1 specs, extracted from that same JAR (`Accounting-1.5.0.json`, `Client Master Data-1.6.0.json`) to `C:\Users\eloadmin\AppData\Local\Temp\2\claude\C--Users-eloadmin-DATEV-Mock\8c106e6e-43c0-4b9c-b0a4-1503db03ef05\scratchpad\datev-openapi\main\mockdata\apispec\`. A compiled, resolved-inline reference for all 18 schema-backed endpoints (exact paths, property names/types/enums, notable quirks) was written to `C:\Users\eloadmin\AppData\Local\Temp\2\claude\C--Users-eloadmin-DATEV-Mock\8c106e6e-43c0-4b9c-b0a4-1503db03ef05\scratchpad\datev-endpoint-specs.md` — **this is the primary contract for Phases A and B below; writers for those phases must read it.** These are scratchpad files (session-local), not part of the repo — if a future session needs them regenerated, re-extract from the JAR per `examples/DATEV_Mock_Server_Reference.md`'s instructions.
- DMS has **no official spec** in the bundle (confirmed via full-text grep, zero "dms" matches) — Phase C is built from the reference doc's example shapes alone, necessarily lower-fidelity.

## Key findings from spec analysis (apply across all phases)
- No OpenAPI `nullable` used anywhere; optionality is purely "absent from `required`".
- All list responses are bare JSON arrays, never a pagination wrapper.
- No real polymorphism (`oneOf`/`discriminator`) — `Addressee`/`creditor`/`debitor` are flat objects with a type enum plus always-present parallel sibling properties for each type (natural_person vs legal_person), gated only by description text.
- A few fields are typed inconsistently or need hardcoded lookup tables instead of enums (`general-ledger-account.main_function*`, `cost-system.number`, `cost-center.cost_rates[].valid_from/valid_to` as integer-encoded dates like `20161201`) — see the compiled spec doc for exact per-field notes.
- `accounts-payable/condense` and `accounts-payable` (and receivable equivalents) share the same schema — condense is a server-side aggregation, not a different shape.

## Cross-phase decisions
- **All new endpoints are JSON-only.** Unlike the original `accounting/v1/clients`, there is no real captured XML evidence for any of these 18+2 endpoints — only JSON evidence (official spec + internal mock's live JSON responses). Inventing an XML shape with no ground truth would be guesswork; don't.
  > **Superseded for the 15 accounting sub-resources** by
  > `odd/tasks/datev-mock-real-data-reconciliation.md` (2026-09-23, W2-W5):
  > once real DATEV traffic showed content negotiation is universal, not
  > `accounting/clients`-specific, all 15 accounting sub-resources gained
  > XML support (confirmed where real capture evidence exists, inferred by
  > a consistent pattern elsewhere — never presented as more certain than
  > that). This entry is kept as-written for historical record of the
  > original decision and its reasoning at the time; Master Data
  > (`addressees`, `banks`) remain JSON-only, unaffected by that epic.
- **No relational filtering by path params.** `{client-id}`, `{fiscal-year-id}`, `{cost-system-id}` in accounting paths are accepted (any value) but do **not** filter/select which fake dataset is returned — every accounting list endpoint always returns the same generic fake fixture regardless of the specific ids in the URL. This mirrors the existing 3 endpoints' behavior and the established "don't implement query-param semantics until a consumer needs them" philosophy (`select`/`filter`/`skip`/`top`/`expand` remain out of scope here too).
- **Exception: `GET /addressees/{addressee-id}` does real lookup-by-id**, returning 404 for an unknown id — this exactly matches an *observed, documented* behavior of the reference mock ("An arbitrary addressee ID returned 404"), so it's evidence-based, not a guess.
- No XML/JSON content negotiation needed for any of these — always `application/json`.
- Admin UI (`/admin`) is **not** extended to cover these new resources in this epic — out of scope, dataset editing stays limited to the original master-data/accounting clients lists unless explicitly requested later.

## Phases (each phase is its own commit+push checkpoint per standing workflow)

### Phase A — Master Data extension (smallest, do first)
3 endpoints: `GET /datev/api/master-data/v1/addressees`, `GET /datev/api/master-data/v1/addressees/{addressee-id}` (real 404 lookup), `GET /datev/api/master-data/v1/banks`. Extends the existing `app/routers/master_data.py` pattern.

- [x] A0 — RED-phase tests
- [x] A1 — Models + fake data (addressees, banks)
- [x] A2 — Router endpoints
- [x] A3 — GREEN verification + commit/push (verification done; commit/push handled separately by orchestrator per standing workflow)

### Phase B — Accounting extension (largest, split into 2 batches for delivery size)
15 endpoints, all under `/datev/api/accounting/v1/clients/{client-id}/fiscal-years/{fiscal-year-id}/...` (exact path spellings per the compiled spec doc). Split by writer-batch, not by meaning, purely to keep each delegation bounded:

**Batch B1**: `fiscal-years` (client-level, not fiscal-year-scoped), `cost-systems`, `cost-systems/{cost-system-id}/cost-centers`, `creditors`, `debitors`, `general-ledger-accounts`.
**Batch B2**: `accounts-payable`, `accounts-payable/condense`, `accounts-receivable/condense`, `accounting-sequences-processed`, `accounting-transaction-keys`, `assets/stocktakings`, `posting-proposal-rules-incoming-invoices`, `posting-proposal-rules-outgoing-invoices`, `terms-of-payment`.

- [x] B0 — RED-phase tests (both batches' worth, written together for consistency — one writer, since test-writing is comparatively lightweight even for 15 endpoints)
- [x] B1-GREEN — Batch 1 implementation (6 endpoints)
- [x] B2-GREEN — Batch 2 implementation (9 endpoints)
- [x] B3 — GREEN verification (full batch) + commit/push (verification done; commit/push handled separately by orchestrator per standing workflow)

### Phase C — DMS (new API area, lower-fidelity spec)
2 endpoints: `GET /datev/api/dms/v1/domains`, `GET /datev/api/dms/v1/documents`. Built from `examples/DATEV_Mock_Server_Reference.md`'s example shapes only (domain/folder/register tree; document metadata incl. amount, class, GUIDs, timestamps) — no OpenAPI spec exists for this area, so field completeness will be lower than Phases A/B. Say so plainly in the eventual README update rather than overclaiming fidelity.

- [x] C0 — RED-phase tests
- [x] C1 — Models + fake data + router
- [x] C2 — GREEN verification + commit/push (verification done; commit/push handled separately by orchestrator per standing workflow)

### Final
- [x] D0 — README update covering all new endpoints, their JSON-only nature, the no-path-filtering decision, and the DMS fidelity caveat. Done by the orchestrator: new "Extended endpoints" table, "Extended endpoint sourcing" section (spec-backed vs self-designed DMS caveat), updated project structure/test counts/roadmap.

## Route
Every phase: delegated direct (RED and GREEN each touch enough files to trigger the Writer rule). Orchestrator runs verification and owns all commits/pushes.

## Progress
- 2026-09-22: Epic created. User chose "start a new epic for all of it" after the settings/admin epic shipped. OpenAPI specs extracted and mapped (18/18 schema-backed endpoints found, DMS confirmed spec-less). Starting with Phase A.
- 2026-09-22: **A0 — RED-phase tests written** (`tests/test_master_data_addressees_banks.py`, 20 tests, one file, in-house style). No existing test file touched; existing 77/77 still pass in isolation. Confirmed correct RED signal: the new file imports `ADDRESSEES_ENDPOINT`/`BANKS_ENDPOINT` directly from `app.routers.master_data`, which don't exist yet, so collection fails with `ImportError: cannot import name 'ADDRESSEES_ENDPOINT' from 'app.routers.master_data'` — this also interrupts the full `pytest tests/` run for the whole suite until A2 adds those constants, so GREEN must add them even before making the endpoints pass.

  **Unambiguous target contract for the GREEN writer (A1/A2):**
  - Paths (exact spelling from spec doc, hyphenated ids): `GET /datev/api/master-data/v1/addressees`, `GET /datev/api/master-data/v1/addressees/{addressee-id}`, `GET /datev/api/master-data/v1/banks`. All JSON-only, `Content-Type: application/json`, bare arrays for the two list endpoints, a bare single object (not a 1-item array) for the by-id endpoint.
  - `app/routers/master_data.py` must define `ADDRESSEES_ENDPOINT = "/datev/api/master-data/v1/addressees"` and `BANKS_ENDPOINT = "/datev/api/master-data/v1/banks"` module-level constants (mirroring the existing `ENDPOINT` constant for `clients`), since the RED tests import them directly.
  - `addressees` list: ≥5 fake records. Each record has non-empty `id` (str), `type` (`natural_person`|`legal_person`), `status` (`active`|`inactive`), `timestamp` (str) always populated. The fake dataset must contain **both** types (tested explicitly, not vacuously). For `type == natural_person` records, `firstname` and `current_surname` must be populated (non-empty str); for `type == legal_person` records, `current_company_name` must be populated (non-empty str). Only these top-level fields are asserted — none of the ~60 nested `detail`/`addresses`/etc. fields are tested in RED, deliberately, per the task scope.
  - `addressees/{addressee-id}`: looking up an id taken from the list response returns 200 + a single JSON object with matching `id`/`type`. Looking up an arbitrary/unknown id (a fresh `uuid4` not in the dataset) returns **404** — this is real lookup-by-id filtering, the sole exception to this epic's "no path-param filtering" rule, and is evidence-based (epic doc: "An arbitrary addressee ID returned 404").
  - `banks` list: ≥5 fake records. Each has non-empty `id` (str — always string, never the bare-number inconsistency the spec's own example shows), `bic` (str, 1–11 chars), `country_code` (str, 1–2 chars), `name` (str, non-empty).
  - All three endpoints must work with zero query params (baseline confirmed by test; no `select`/`filter`/`skip`/`top`/`expand` semantics implemented, consistent with the rest of the epic).

  **Judgment calls / ambiguities flagged for GREEN and future reviewers:**
  - The compiled spec doc only explicitly marks `type` as `REQUIRED` on `Addressee` (`id`'s "required" note is about PUT request bodies, not GET responses). RED nonetheless treats `id`/`status`/`timestamp` as reliably-present in every fake record — a deliberate mock-generation choice for a coherent dataset, not something the OpenAPI schema strictly mandates.
  - Requiring `firstname`+`current_surname` (natural_person) / `current_company_name` (legal_person) to always be populated per-type is likewise a RED-imposed fake-data contract, not an OpenAPI `required` constraint (the spec gates these fields by free-text description only, both are technically optional for either type). GREEN must follow this contract for the tests to pass; it does not need to leave the *other* type's fields null/absent (untested either way).
  - The 404-unknown-id test uses a freshly generated `uuid4` and asserts it isn't already a known id first — the actual collision probability is negligible, not a real flake risk.

- 2026-09-22: **A1/A2/A3 — GREEN.** Added `Addressee`/`Bank` dataclasses (`app/models.py`, flat top-level fields only — `detail`/`addresses`/`communications`/`bank_accounts`/`tax_offices`/`contact_persons` intentionally not modeled or served, consistent with the real API omitting them whenever `expand` isn't requested, which this mock never implements). Added `_generate_addressees()`/`_generate_banks()` to `app/fake_data.py` (8 addressees — forced mix of both `natural_person`/`legal_person` at indices 0/1, rest random; 6 banks — fictitious invented BIC/name/city/country_code combinations, no real bank identifiers). Added `list_addressees()`/`get_addressee(id)`/`list_banks()` (read-only, no CRUD — out of scope per epic doc) to `app/data_store.py`, wired into `reset()`. Added `ADDRESSEES_ENDPOINT`/`BANKS_ENDPOINT` constants and the three GET routes to `app/routers/master_data.py`, returning plain dicts/lists (FastAPI's default JSON response) with `None`-valued optional fields dropped before serialization (matches the spec's no-`nullable` convention). `addressees/{addressee_id}` does real lookup-by-id, raising `HTTPException(404)` on miss. Files touched: `app/models.py`, `app/fake_data.py`, `app/data_store.py`, `app/routers/master_data.py`. `tests/test_master_data_addressees_banks.py` was not modified.
  - **Test count note (orchestrator-investigated):** the GREEN writer reported "74 base tests" and flagged a discrepancy against the expected 77. Verified independently: the base suite is in fact exactly 77 (8+7+7+10+9+18+18 across the 7 pre-existing files), and the new file actually has **17** tests, not the 20 the writer's report claimed — 77+17=94 matches the real passing count exactly. The writer's headline numbers were simply a self-reporting miscount, not a real gap; nothing was missing or broken.
  - **Stray process found and fixed (orchestrator):** the port-58452 conflict the writer hit (PID 4400) was not "unrelated" — it was a leaked `uvicorn` process from an earlier verification step in the settings epic, where `kill <bash-job-id>` had only killed the shell job wrapper, not the actual Windows process, leaving stale old code listening on the mock's real port. Force-killed via `taskkill /F /PID`. Lesson applied to this task's own verification: killed the server by its actual reported "Started server process [PID]" via `taskkill`, not by shell job id, and confirmed the port was released afterward.
  - **Data-quality fix (orchestrator):** live-checking the addressees endpoint surfaced a name/sex mismatch (e.g. "Sabine" — a female name — tagged `sex: "male"`), because `sex` was cycling through a pool by loop index, unrelated to which name was actually picked. Fixed by adding an explicit `_FIRST_NAME_SEX` lookup keyed by first name (all 15 names in `_PERSON_NAME_POOL` mapped correctly) and using that instead of the index-cycling `_SEX_POOL`, which is now removed as dead code. Re-verified: 94/94 still passing after the fix.
  - **Final verification:** `.venv\Scripts\python.exe -m pytest tests/ -v` → **94 passed**, independently re-run by the orchestrator after the fix above. Live sanity check on the correct port 58452 (after clearing the stray process): `GET /datev/api/master-data/v1/addressees` → 200, JSON array, ≥5 records, both types present, names/sex now consistent; `GET /datev/api/master-data/v1/addressees/{known-id}` → 200, matching object; `GET /datev/api/master-data/v1/addressees/00000000-0000-0000-0000-000000000000` (unknown id) → 404; `GET /datev/api/master-data/v1/banks` → 200, JSON array, 6 plausible fictitious records. Server stopped cleanly, port confirmed released. Phase A complete; committed and pushed per the standing "commit+push when an epic/phase finishes" instruction.

- 2026-09-22: **B0 — RED-phase tests written** for all 15 Phase B endpoints, split into 4 files (54 tests total, in-house style, one writer):
  - `tests/test_accounting_fiscal_structure.py` (10 tests) — `fiscal-years`, `cost-systems`, `cost-systems/{cost-system-id}/cost-centers`.
  - `tests/test_accounting_payables_receivables.py` (10 tests) — `accounts-payable`, `accounts-payable/condense`, `accounts-receivable/condense`.
  - `tests/test_accounting_partners.py` (10 tests) — `creditors`, `debitors`.
  - `tests/test_accounting_misc.py` (24 tests) — `accounting-sequences-processed`, `accounting-transaction-keys`, `assets/stocktakings`, `general-ledger-accounts`, `posting-proposal-rules-incoming-invoices`, `posting-proposal-rules-outgoing-invoices`, `terms-of-payment`.

  No existing test file was touched. Confirmed correct RED signal: all 4 new files import `*_ENDPOINT` constants directly from `app.routers.accounting`, none of which exist yet (only the existing XML `clients` endpoint's `ENDPOINT` constant is defined there), so collection fails with e.g. `ImportError: cannot import name 'COST_CENTERS_ENDPOINT' from 'app.routers.accounting'` (one such error per new file, 4 total) — this interrupts the full `pytest tests/` run for the whole suite until B1/B2-GREEN add these constants. Re-running the full suite with the 4 new files excluded (`--ignore=...` x4) confirms the pre-existing 94/94 still pass in isolation, unaffected.

  **Unambiguous target contract for the GREEN writers (B1-GREEN batch 1: `fiscal-years`, `cost-systems`, `cost-centers`, `creditors`, `debitors`, `general-ledger-accounts`; B2-GREEN batch 2: the remaining 9):** all paths below are exact literal spellings from the compiled spec doc, hyphenated in the URL; path-param placeholders shown as Python `str.format()` tokens matching FastAPI's own `{name}` route-decorator syntax (snake_case, not the doc's hyphenated `{client-id}` spelling, mirroring Phase A's `{addressee_id}` precedent in `master_data.py`). Every endpoint below is JSON-only, `Content-Type: application/json`, bare array response, and must ignore its path-param values entirely (same fixed fake dataset regardless of `client_id`/`fiscal_year_id`/`cost_system_id` — locked in by an `*_ignores_*_id_values` test per endpoint, asserting byte-for-byte equal JSON across two calls with different arbitrary ids).

  | # | Constant | Path template |
  |---|----------|----------------|
  | 1 | `FISCAL_YEARS_ENDPOINT` | `/datev/api/accounting/v1/clients/{client_id}/fiscal-years` |
  | 2 | `ACCOUNTS_PAYABLE_ENDPOINT` | `/datev/api/accounting/v1/clients/{client_id}/fiscal-years/{fiscal_year_id}/accounts-payable` |
  | 3 | `ACCOUNTS_PAYABLE_CONDENSE_ENDPOINT` | same prefix + `/accounts-payable/condense` |
  | 4 | `ACCOUNTS_RECEIVABLE_CONDENSE_ENDPOINT` | same prefix + `/accounts-receivable/condense` |
  | 5 | `ACCOUNTING_SEQUENCES_PROCESSED_ENDPOINT` | same prefix + `/accounting-sequences-processed` |
  | 6 | `ACCOUNTING_TRANSACTION_KEYS_ENDPOINT` | same prefix + `/accounting-transaction-keys` |
  | 7 | `ASSETS_STOCKTAKINGS_ENDPOINT` | same prefix + `/assets/stocktakings` |
  | 8 | `COST_SYSTEMS_ENDPOINT` | same prefix + `/cost-systems` |
  | 9 | `COST_CENTERS_ENDPOINT` | same prefix + `/cost-systems/{cost_system_id}/cost-centers` |
  | 10 | `CREDITORS_ENDPOINT` | same prefix + `/creditors` |
  | 11 | `DEBITORS_ENDPOINT` | same prefix + `/debitors` |
  | 12 | `GENERAL_LEDGER_ACCOUNTS_ENDPOINT` | same prefix + `/general-ledger-accounts` |
  | 13 | `POSTING_PROPOSAL_RULES_INCOMING_INVOICES_ENDPOINT` | same prefix + `/posting-proposal-rules-incoming-invoices` |
  | 14 | `POSTING_PROPOSAL_RULES_OUTGOING_INVOICES_ENDPOINT` | same prefix + `/posting-proposal-rules-outgoing-invoices` |
  | 15 | `TERMS_OF_PAYMENT_ENDPOINT` | same prefix + `/terms-of-payment` |

  ("same prefix" = `/datev/api/accounting/v1/clients/{client_id}/fiscal-years/{fiscal_year_id}`.)

  Per-endpoint fake-data contract asserted by the RED tests (minimums are a judgment call — see ambiguities below):
  - `fiscal-years` (≥2 records): `id` is an 8-digit `YYYYMMDD` string; `account_system` int; `currency_code` str (1–3 chars); `legal_form` in the documented 13-value enum; `taxation_method` in the documented 6-value enum; `national_right` in `{"DE","AT"}`; `is_locked` bool.
  - `cost-systems` (≥2 records): `id` str of length 1; `short_name` non-empty str; `is_activated_for_postings` bool; `number` int (spec types it `number`/"short" despite being integer-valued — GREEN must emit a plain JSON integer).
  - `cost-centers` (≥3 records): `id`/`long_name`/`short_name`/`creation_date` non-empty strings; at least one record's `cost_rates[]` populated, with `valid_from`/`valid_to` as **integers** matching `YYYYMMDD` (e.g. `20161201`, not a date-time string) and `rate` numeric.
  - `accounts-payable` / `accounts-payable/condense` (≥3 records each, same schema): `id` non-empty str; `account_number` int; `amount_debit`/`amount_credit`/`amount_entered`/`open_balance_of_item` numeric; `currency_code` non-empty str; `evidence_type` in the documented 6-value enum; `debit_credit_identifier` in `{"S","H"}`; `is_cleared` bool. Both endpoints must work independently for the same ids (tested), without requiring identical bodies.
  - `accounts-receivable/condense` (≥3 records): same base contract as accounts-payable, plus `dunning_level` non-empty str on every record (RED-imposed fake-data contract — the OpenAPI schema doesn't mark it required, see ambiguities).
  - `accounting-sequences-processed` (≥3 records): `id`/`accounting_sequence_id`/`description`/`date_from`/`date_to` non-empty strings; `is_committed` bool; `record_type` in `{financial_accounting, annual_financial_statements}`; `accounting_reason` in the documented 7-value enum.
  - `accounting-transaction-keys` (≥3 records): `id` non-empty str; `number` int in `[1, 9999]`; `tax_rate` numeric in `[0, 99.99]`; `is_tax_rate_selectable` bool.
  - `assets/stocktakings` (≥3 records): `asset_number` int and `inventory_number` non-empty str always populated (the spec's own two `required` fields); `accounting_reason` in `{50, 30, 40, 64, 11, 12}`; at least one record's `general_ledger_account` populated with `account_number` int + `caption` non-empty str.
  - `cost-systems/.../cost-centers` — see above.
  - `creditors` / `debitors` (≥3 records each): `id`/`addressee_id`/`business_partner_number`/`short_name` non-empty strings; `account_number` int; `legal_entity_type` in `{not_specified, natural_person, legal_person}`; dataset must contain **both** `natural_person` and `legal_person` records (tested explicitly, same non-vacuous-enum pattern as Phase A's addressees); `natural_person` records need `natural_person.firstname`/`natural_person.surname` populated (creditors only — see ambiguities), `legal_person` records need `legal_person.legal_name` populated (creditors only).
  - `general-ledger-accounts` (≥5 records): `id` non-empty str; `account_number` int; `caption` non-empty str; `main_function` in `{1..7}`; `main_function_number` in `{10, 11, 12, 20, 21, 25, 90, 91, 98}` — both hardcoded lookup sets per the epic doc's explicit quirk note (spec types them as plain `integer`, no OpenAPI `enum`).
  - `posting-proposal-rules-incoming-invoices` / `-outgoing-invoices` (≥3 records each): `id` non-empty str; `uncertain_label` bool; `assignment_criteria.name` non-empty str + `assignment_criteria.tax_rate` numeric; `posting_proposal_information` non-empty list, each entry's `accounting_transaction_key` int in `[1, 9999]`, `account_number` int, and `origin_of_posting_description` in the 5-value (incoming) or 7-value (outgoing, +`email`/`transaction_key`) enum.
  - `terms-of-payment` (≥3 records): `caption` non-empty str (the spec's sole `required` field) on every record; `due_type` in `{due_in_days, due_as_period}`; dataset must contain **both** variants (non-vacuous, same pattern as above); `due_in_days` records need `due_in_days.due_in_days` int populated; `due_as_period` records need `due_as_period.period1.invoice_day_of_month` int and `due_as_period.period1.due_date_net.related_month`/`day_of_month` populated (`related_month` in `{current_month, next_month, month_after_next}`).

  **Judgment calls / ambiguities flagged for GREEN and future reviewers:**
  - Minimum record counts per endpoint are a RED-imposed judgment call, not spec-derived: small/bounded-domain resources got small minimums (`fiscal-years` ≥2, `cost-systems` ≥2 — real DATEV cost systems are single-char-id, so effectively ≤10 possible), most transactional/master-like lists got ≥3, and the plausibly-largest resource (`general-ledger-accounts`, a chart of accounts) got ≥5. GREEN is free to generate more; these are floors, not targets.
  - `accounts-receivable/condense.dunning_level` being asserted non-empty on every record is a RED-imposed fake-data contract (like Phase A's addressee type-specific fields) — the OpenAPI schema doesn't mark it required, and real dunning data would often be genuinely absent for a non-dunned item. GREEN must populate it on every fake record for the tests to pass.
  - `creditors`/`debitors` both got a "both `legal_entity_type` values present" test (mirroring Phase A's addressee precedent), but only `creditors` got the deeper `natural_person.firstname`/`surname` and `legal_person.legal_name` populated-field assertions — `debitors` is documented in the spec doc as structurally identical to `creditors` for these shared sub-objects, so re-asserting the same nested-field contract there would be redundant coverage, not a different mock-generation instruction. GREEN must still populate `debitor.natural_person`/`legal_person` consistently with `debitor.legal_entity_type`, the same way `creditor` does, even though RED doesn't test it directly for debitors.
  - `debitor`'s much larger `accounting_information` sub-schema (dunning/credit-limit/direct-debit/account-statement fields, ~30 properties per the spec doc) is intentionally **not** asserted at all by RED — only the shared top-level business-partner fields are tested for both `creditor` and `debitor`. GREEN has full latitude on `debitor.accounting_information`'s fake-data shape as long as the object is present and internally plausible; no RED test locks in specific sub-field values there.
  - `general-ledger-account.additional_function` (documented `{0, 1, 3, 8}` per the epic doc) is **not** asserted by RED — only `main_function`/`main_function_number` are, since those two were the ones explicitly flagged as a quirk in the epic doc's "ground truth" notes. GREEN should still populate `additional_function` plausibly (one of the four documented values) for shape-completeness, even without a locking test.
  - This mock does not implement streaming (no consumer request for it), so the spec's streaming-only field-drop quirk (`open_balance_of_item`/`debit_credit_identifier` omitted when streamed, paging unsupported while streaming) was deliberately **not** modeled — RED always expects both fields present. If streaming is ever requested in a future phase, this is a compatible additive change (a query param toggling field omission), not a contradiction of these tests.
  - `cost-center.properties[]` (the Kostenrechnung-classic-only nested array alongside `cost_rates[]`) is **not** asserted by RED at all — only `cost_rates[].valid_from`/`valid_to`/`rate` are tested, since those carry the explicit integer-encoded-date quirk called out in the epic doc. GREEN may populate `properties[]` or leave it empty/absent; no test depends on it.

- 2026-09-22: **B1-GREEN — Batch 1 implementation.** Implemented the 6 batch-B1 endpoints only (`fiscal-years`, `cost-systems`, `cost-systems/{cost_system_id}/cost-centers`, `creditors`, `debitors`, `general-ledger-accounts`), following Phase A's dataclass/fake-data/data-store/router pattern; batch B2's 9 endpoints (`accounts-payable`, `accounts-payable/condense`, `accounts-receivable/condense`, `accounting-sequences-processed`, `accounting-transaction-keys`, `assets/stocktakings`, `posting-proposal-rules-incoming-invoices`, `posting-proposal-rules-outgoing-invoices`, `terms-of-payment`) were intentionally left untouched.
  - **Models** (`app/models.py`): added `FiscalYear`, `CostSystem`, `CostRate`, `CostCenter` (with a nested `cost_rates: list[CostRate]`, `valid_from`/`valid_to` kept as `int`), `NaturalPerson`/`LegalPerson`/`NotSpecifiedPerson` (shared by creditor/debitor, mirroring the spec's discriminator-free convention), `CreditorAccountingInformation`/`DebitorAccountingInformation` (populated for shape-completeness only, unasserted by RED), `Creditor`, `Debitor`, `GeneralLedgerAccountTaxRate`, `GeneralLedgerAccount`.
  - **Fake data** (`app/fake_data.py`): added `_generate_fiscal_years` (3 records), `_generate_cost_systems` (3), `_generate_cost_centers` (4, cost_rates populated on the even-indexed records with integer-encoded `YYYYMMDD` `valid_from`/`valid_to`), `_generate_creditors`/`_generate_debitors` (4 each, indices 0/1 forced to `natural_person`/`legal_person` respectively — same non-vacuous-mix pattern as Phase A's addressees, drawing names from the existing `_PERSON_NAME_POOL`/`_ORG_NAME_POOL`), `_generate_general_ledger_accounts` (6, `main_function`/`main_function_number`/`additional_function` all drawn from hardcoded lookup lists — `_MAIN_FUNCTION_VALUES = [1..7]`, `_MAIN_FUNCTION_NUMBER_VALUES = [10,11,12,20,21,25,90,91,98]`, `_ADDITIONAL_FUNCTION_VALUES = [0,1,3,8]` — never an open `randint` range, per the epic doc's explicit quirk note). All generators wired into module-level constants and `reset()`.
  - **Data store** (`app/data_store.py`): added read-only `list_fiscal_years()`/`list_cost_systems()`/`list_cost_centers()`/`list_creditors()`/`list_debitors()`/`list_general_ledger_accounts()` (no CRUD — matches Phase A's precedent for non-admin-edited resources, and the epic doc's "no path-param filtering" decision means these listers correctly take no arguments), wired into `reset()`.
  - **Router** (`app/routers/accounting.py`): added the 6 `*_ENDPOINT` constants (exact names/paths per the B0 contract table, snake_case FastAPI path params matching Phase A's `master_data.py` precedent, not the compiled spec doc's hyphenated spelling) and their GET handlers. Every handler declares its path params (`client_id`/`fiscal_year_id`/`cost_system_id`) as FastAPI-required function arguments (so route registration succeeds) but never reads them — same fixed dataset regardless of URL value, satisfying the `*_ignores_*_id_values` RED tests. Added a recursive `_strip_none`/`_to_json` pair (module-local, `dataclasses.asdict` + recursive `None`-drop) instead of reusing `master_data.py`'s shallow `_to_json`, since these new dataclasses nest (`cost_rates[]`, `natural_person`/`legal_person`, `tax_rates[]`) and a top-level-only strip would have left stray `null`s inside nested objects/lists.
  - **Test results:** targeted run `pytest tests/test_accounting_fiscal_structure.py tests/test_accounting_partners.py -v` → **20 passed**. Full suite `pytest tests/ -v` (no `--ignore`) correctly **interrupts at collection** with exactly 2 errors (`test_accounting_misc.py` now failing on `ImportError: cannot import name 'ACCOUNTING_SEQUENCES_PROCESSED_ENDPOINT'`, `test_accounting_payables_receivables.py` on `ImportError: cannot import name 'ACCOUNTS_PAYABLE_CONDENSE_ENDPOINT'`) — both batch-B2 files, untouched, still cleanly RED on collection exactly as before (the specific first-missing-name shifted because 6 of the 15 constants now exist, but it's still a pure collection-time `ImportError`, so nothing in either file was partially satisfied). Re-running with those 2 files excluded (`--ignore=tests/test_accounting_misc.py --ignore=tests/test_accounting_payables_receivables.py`) confirms **114 passed** (94 pre-existing + 20 new B1 tests), 0 failed. No test file was modified.
  - Files created: none (all changes were additions to existing modules). Files modified: `app/models.py`, `app/fake_data.py`, `app/data_store.py`, `app/routers/accounting.py`.
  - Not touched (batch B2's job): `tests/test_accounting_payables_receivables.py`, `tests/test_accounting_misc.py`.
  - Git: not touched, per instructions — commit/push left to the orchestrator per the standing workflow.

- 2026-09-22: **B2-GREEN / B3 — Batch 2 implementation + GREEN verification.** Implemented the remaining 9 batch-B2 endpoints (`accounts-payable`, `accounts-payable/condense`, `accounts-receivable/condense`, `accounting-sequences-processed`, `accounting-transaction-keys`, `assets/stocktakings`, `posting-proposal-rules-incoming-invoices`, `posting-proposal-rules-outgoing-invoices`, `terms-of-payment`). All target dataclasses (`OpenItem`, `AccountingSequenceProcessed`, `AccountingTransactionKey`, `GeneralLedgerAccountMinimal`, `AssetStocktaking`, `AssignmentCriteria`, `PostingProposalInformation`, `PostingProposalRule`, `DueDate`, `Period`, `DueInDays`, `DueAsPeriod`, `TermOfPayment`) already existed in `app/models.py` from a prior session and were reused as-is, unmodified.
  - **Fake data** (`app/fake_data.py`): added `_generate_open_items(count, receivable)` — shared generator for `accounts-payable`/`accounts-payable/condense` (called twice, `receivable=False`) and `accounts-receivable/condense` (`receivable=True`, also populates `dunning_level` on every record per the RED-imposed contract); `_generate_accounting_sequences_processed`, `_generate_accounting_transaction_keys`, `_generate_asset_stocktakings` (index-0/2 get a populated `general_ledger_account`), `_generate_posting_proposal_rules(count, outgoing)` — shared generator for both invoice directions, differing only in the `origin_of_posting_description` enum pool, `_generate_terms_of_payment` (even indices `due_in_days`, odd indices `due_as_period`, guaranteeing both variants present, same non-vacuous-mix pattern as `_generate_addressees`/`_generate_creditors`). All hardcoded enum/lookup sets (`evidence_type`, `debit_credit_identifier`, `accounting_reason` x2 different sets for sequences vs. stocktakings, `record_type`, `origin_of_posting_description` incoming/outgoing, `due_type`, `related_month`) match the task doc's contract table exactly. Each generator called with `count=4` (above every RED floor of 3, general-ledger-accounts' pre-existing floor of 5 untouched). All wired into module-level constants and `reset()`.
  - **Data store** (`app/data_store.py`): added read-only `list_accounts_payable()`, `list_accounts_payable_condense()`, `list_accounts_receivable_condense()`, `list_accounting_sequences_processed()`, `list_accounting_transaction_keys()`, `list_assets_stocktakings()`, `list_posting_proposal_rules_incoming_invoices()`, `list_posting_proposal_rules_outgoing_invoices()`, `list_terms_of_payment()` — all no-argument (no path-param filtering, matching every prior lister), wired into `reset()`.
  - **Router** (`app/routers/accounting.py`): added the 9 `*_ENDPOINT` constants and their GET handlers, reusing the existing `_strip_none`/`_to_json` pair. Every handler declares `client_id`/`fiscal_year_id` as required FastAPI path params but never reads them, matching the `*_ignores_*_id_values` RED tests. `accounts-payable/condense` and `accounts-payable` are distinct static path suffixes (`/accounts-payable` vs. `/accounts-payable/condense`) so there's no FastAPI route-ordering ambiguity between them.
  - **Test results:** targeted `pytest tests/test_accounting_payables_receivables.py tests/test_accounting_misc.py -v` → **34 passed** on the first run (no RED->GREEN iteration needed). Full suite `pytest tests/ -v` → **148 passed**, 0 failed (114 pre-existing + 34 new B2 tests) — matches the expected count exactly. No test file was modified.
  - **Live sanity check:** started `uvicorn` on `127.0.0.1:58452` with the existing certs (`certs/key.pem`/`certs/cert.pem`); reported "Started server process [11196]". Curled 4 of the 9 new endpoints (`accounts-payable`, `accounting-transaction-keys`, `terms-of-payment`, `assets/stocktakings`) — all returned `200`, `Content-Type: application/json`, well-formed bare JSON arrays, `None`-valued optional fields correctly stripped (e.g. stocktaking records without a `general_ledger_account` simply omit the key). Stopped the server via `taskkill /F /PID 11196` (the actual reported server PID, not the shell background-job id), then confirmed via `netstat -ano | grep :58452` that no TCP listener remained (only an unrelated pre-existing UDP entry on a different PID).
  - Files modified: `app/fake_data.py`, `app/data_store.py`, `app/routers/accounting.py` (no changes needed to `app/models.py` — all target dataclasses were already correct and complete from the prior session). No test file was modified.
  - Git: not touched, per instructions — commit/push left to the orchestrator per the standing workflow. All 15 Phase B endpoints (6 from B1 + 9 from B2) are now collectively implemented and GREEN.

- 2026-09-22: **C0 — RED-phase tests written** (`tests/test_dms.py`, 17 tests, one file, in-house style). No existing test file touched; the pre-existing 148/148 still pass in isolation (`pytest tests/ --ignore=tests/test_dms.py`). Confirmed correct RED signal: the new file imports `DOMAINS_ENDPOINT`/`DOCUMENTS_ENDPOINT` directly from `app.routers.dms`, a module that does not exist yet at all (unlike Phases A/B, which extended an already-existing router file), so collection fails with `ModuleNotFoundError: No module named 'app.routers.dms'` — this also interrupts the full `pytest tests/` run for the whole suite until C1 creates that module, same pattern as every prior phase.

  **Explicit fidelity caveat (per the epic doc's own Phase C note):** DMS has zero official spec coverage — confirmed via full-text grep of both bundled OpenAPI files (zero "dms" matches). The *only* ground truth is two one-line rows in `examples/DATEV_Mock_Server_Reference.md`'s "DMS API" table: `GET /datev/api/dms/v1/domains` → "Domain/folder/register tree, e.g. `Unternehmen`" and `GET /datev/api/dms/v1/documents` → "Document metadata including amount, class, GUIDs, and timestamps." Everything below the endpoint paths and those two phrases is a **self-designed, invented schema** — a deliberate, explicitly-flagged exception to this epic's normal "never guess, only build from confirmed evidence" rule. No comparable hierarchical/tree resource exists elsewhere in the reference doc to crib field names from (the closest analog, `cost-centers`, is a flat list with no parent/child relationship), so the tree shape is common-sense DMS design, not extracted evidence. This is documented in `test_dms.py`'s module docstring as well, for the GREEN writer and any future reviewer.

  **Self-designed schema (target contract for the GREEN writer, C1):**
  - `GET /datev/api/dms/v1/domains` (≥5 fake records, JSON-only, bare array): modeled as a **flat list with parent references** (`parent_id`), not deep JSON nesting — chosen because it's trivially testable and a completely standard adjacency-list way to represent a tree over HTTP. Fields: `id` (str, non-empty), `name` (str, non-empty — e.g. "Unternehmen" per the doc's own example, or invented folder/register names like "Belege"/"Rechnungseingang"), `type` (`domain`|`folder`|`register` — the three levels named literally in the description, so the least speculative part of the design). `parent_id` (str, optional): **absent** (not `null`) on root `type == "domain"` records, present and referencing another record's `id` on every `folder`/`register` record — matches this epic's established "optionality = field absence, never `nullable`" convention. Dataset must contain **at least 2 distinct tree levels** (tested non-vacuously, same pattern as Phase A/B's type-mix tests).
  - `GET /datev/api/dms/v1/documents` (≥5 fake records, JSON-only, bare array): per the description's four named facets (amount, class, GUIDs, timestamps) — `id` (str, must be UUID-parseable — the description says "GUIDs" plural, so this is the one field asserted stricter than a plain non-empty string), `name` (str, non-empty — an obviously-necessary title/filename, not in the description but judged essential for a minimally useful record), `amount` (numeric, asserted `> 0`), `document_class` (str, small invented enum: `invoice`|`receipt`|`contract`|`delivery_note` — no real DATEV vocabulary exists anywhere in this repo's evidence for this field), `domain_id` (str, must reference a known `id` from the `domains` response — ties the two endpoints together sensibly, judged obviously necessary rather than inventing an unrelated field), `created_at`/`modified_at` (str timestamps — plural "timestamps" in the description justifies two fields, not one). Dataset must contain **at least 2 distinct `document_class` values** (non-vacuous, same pattern as the domain-level-mix test).
  - Both endpoints confirmed to work with zero query params (baseline test each), consistent with the rest of the epic's "no query-param semantics implemented" stance.

  **Judgment calls / ambiguities flagged for GREEN and future reviewers:**
  - Every field name, the tree-as-flat-list-with-parent_id design choice, the 3-level `type` enum, the 4-value `document_class` enum, and the `domain_id` cross-reference are all self-designed by the RED writer, not derived from any spec or captured example payload — this whole phase is intentionally lower-fidelity than A/B, as flagged in the epic doc from the start. Field completeness here should not be assumed comparable to Phases A/B's spec-backed schemas.
  - Because nothing here is spec-verified, RED deliberately asserts *shape and types* (str/numeric/enum-membership/GUID-parseable/cross-reference-resolves), never a specific invented value (no hardcoded fake name, amount, or timestamp string is asserted anywhere) — leaving room for GREEN to pick whatever concrete fake values it likes within the documented contract.
  - `amount` is only asserted to be a positive number; no currency field was invented (the description says only "amount", not "amount + currency", and Phase B's `currency_code` precedent lives on a different, spec-backed resource — inventing one here would be scope creep beyond the two described facets).
  - GREEN is free to add more fields to either record type (e.g. a `status`, a `path`, a `content_type`) for shape-completeness; no RED test locks the schema to *only* the fields listed above — the tests only lock in what must be present, not an exhaustive/closed schema.

- 2026-09-22: **C1/C2 — GREEN.** Added `Domain`/`Document` dataclasses (`app/models.py`, flat fields only, matching the self-designed C0 contract exactly: `Domain.parent_id` optional/absent-not-null; `Document.id`/`amount`/`document_class`/`domain_id`/`created_at`/`modified_at` all required). Added `_generate_domains()`/`_generate_documents()` to `app/fake_data.py`: `_generate_domains()` returns a fixed, hand-authored adjacency-list tree (`_DMS_DOMAIN_TREE`, 6 nodes — 1 root `domain` "Unternehmen", 2 `folder`s "Belege"/"Rechnungswesen", 3 `register`s "Rechnungseingang"/"Rechnungsausgang"/"Kontoauszüge") rather than randomly generated, since a coherent hand-authored tree reads better than a randomized one for a resource this small; `_generate_documents(count=6)` cycles through 4 invented `document_class` values and all 6 domain-tree node ids so every document's `domain_id` resolves to a real node (no dangling references), `created_at`/`modified_at` via the existing `_random_timestamp()`/`_random_date_after()` helpers. Both wired into module-level `DOMAINS`/`DOCUMENTS` constants and `reset()`. Added read-only `list_domains()`/`list_documents()` to `app/data_store.py` (no CRUD, no path-param filtering — DMS endpoints don't even have path params), wired into `reset()`. Created `app/routers/dms.py` (new router file, unlike Phases A/B which extended an existing one) with `DOMAINS_ENDPOINT`/`DOCUMENTS_ENDPOINT` constants (exact paths per contract) and the two GET handlers, reusing `master_data.py`'s shallow `_to_json` pattern (dataclass -> dict, `None`-valued optional fields dropped) since neither `Domain` nor `Document` nest. Mounted `dms.router` in `app/main.py` alongside the existing four routers. Docstrings on the new model/fake-data/router code all carry the fidelity caveat forward (self-designed schema, no official DMS spec exists), per the C0 entry's explicit instruction.
  - **Test results:** `pytest tests/test_dms.py -v` → **17 passed** on the first run (no RED->GREEN iteration needed). Full suite `pytest tests/ -v` → **165 passed**, 0 failed (148 pre-existing + 17 new) — matches the expected count exactly. `tests/test_dms.py` was not modified.
  - **Live sanity check:** started `uvicorn` on `127.0.0.1:58452` with the existing certs; reported "Started server process [25944]". `GET /datev/api/dms/v1/domains` → 200, `content-type: application/json`, bare JSON array of 6 nodes, root has no `parent_id` key, folders/registers all carry a `parent_id` resolving to a real sibling `id`. `GET /datev/api/dms/v1/documents` → 200, bare JSON array of 6 records, GUID `id`s, positive `amount`s, varied `document_class`/`domain_id` values, both timestamp fields populated. Stopped the server via `taskkill /F /PID 25944` (the actual reported server PID, not the shell background-job id — the lesson from Phase A/B's progress notes applied again). Confirmed via `netstat -ano | grep :58452` that no TCP listener remained afterward (only an unrelated UDP entry on the shell job's own PID, same benign pattern noted in the B2-GREEN entry).
  - Files created: `app/routers/dms.py`. Files modified: `app/models.py`, `app/fake_data.py`, `app/data_store.py`, `app/main.py`. No test file was modified.
  - Git: not touched, per instructions — commit/push left to the orchestrator per the standing workflow. Phase C is now fully GREEN; only the epic-level `D0` README update remains.

- 2026-09-22: **D0 — README updated, epic complete.** Orchestrator independently re-verified 165/165 passing and confirmed no leaked server process before writing docs. README changes: new "Extended endpoints" table (Master Data/Accounting/DMS grouped separately from the base 3), new "Extended endpoint sourcing" section explaining the two evidence tiers (OpenAPI-spec-backed for Master Data + Accounting vs. self-designed for DMS, with the specific quirks — `main_function`/`main_function_number` lookup sets, integer-encoded `cost_rates` dates, shared `accounts-payable`/`condense` schema — called out explicitly), updated project structure diagram (`dms.py` router, updated file comments), updated test counts (165) throughout, and an updated roadmap section listing all three epics with all phases checked off. All three task docs (`datev-mock.md`, `datev-mock-settings.md`, `datev-mock-extended-endpoints.md`) are cross-linked from the README. **Epic complete** — committed and pushed per the standing "commit+push when an epic/phase finishes" instruction. Note for any future session picking this back up: an earlier B2 attempt in this epic was interrupted mid-way by an API rate limit — the resumed session found the partial state (models complete, router/fake-data/data-store wiring incomplete) via `git diff --stat` and `python -c "import ..."` sanity checks before resuming, rather than assuming either "nothing was done" or "everything was done"; worth the same care if this ever recurs.
