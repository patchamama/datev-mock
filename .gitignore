.venv/
python-portable/
__pycache__/
*.pyc
*.pyo
certs/*.pem
.pytest_cache/
settings.json
datev_mock.db
datev_mock.db-journal
datev_mock.db-wal
datev_mock.db-shm

# Contains sensitive real DATEV data captured from a live environment — never publish
examples/
test-datev.html

# Real customer ELO/DATEV connector config and mapping references — never publish
docs/

# Local tooling cache
.atl

.env
.env.py

# Standalone Java sub-project with its own Git repository (spring-boot/.git)
# -- never tracked or staged from the FastAPI repo's own index.
spring-boot/

