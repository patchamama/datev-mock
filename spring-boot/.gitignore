# Maven build output
target/

# Project-local Maven repository (optional -Dmaven.repo.local target)
.m2/

# Maven Wrapper distribution cache
.mvn/wrapper/maven-wrapper.jar
.mvn/wrapper/dists/

# Portable JDK 21 bootstrapped by the root start_java_datev_mock.bat/.sh
# launchers when no usable system Java 21+ is found (project-local, never
# installed system-wide)
.jdk21-portable/

# IDE metadata
.idea/
*.iml
.project
.classpath
.settings/

# OS files
.DS_Store
Thumbs.db

# Runtime local state (SB4: SQLite write-overlay store)
datev_mock.db
datev_mock.db-journal
datev_mock.db-wal
datev_mock.db-shm

# Runtime local state (SB9: admin-editable settings persistence)
settings.json

# Runtime local state (resource snapshot export -- see
# odd/tasks/datev-mock-resource-snapshot-export.md)
snapshots/